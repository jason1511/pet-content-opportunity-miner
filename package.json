{
  "name": "pet-growth-platform",
  "version": "1.0.0",
  "description": "Pet growth research workflows and customer-facing utility tools on Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node scripts/smoke-check.mjs && node --test scripts/*.test.mjs",
    "deploy": "wrangler deploy",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
